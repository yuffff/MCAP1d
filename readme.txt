

g.compile multi. cpp files:
    g++ -c myclass.cpp
    g++ -c main.cpp
    g++ myclass.o main.o
    ./a.out

solved:compile multiple cpp files 
    1.GEN obj files in bin dir 
    2.select and run with RUN bat (open an other window)